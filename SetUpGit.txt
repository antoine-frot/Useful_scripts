# Set up the SSH key
ssh-keygen -t ed25519 -C "antoine.frot@orange.fr" (Use default)
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
cat ~/.ssh/id_ed25519.pub
git remote set-url origin git@github.com:antoine-frot/Useful_scripts

# Set up the configuration files
cp ~/.bashrc bash_utility/configuration_files/bashrc_name_of_the_machine
for configuration_file in (bashrc bash_profile bash_logout vimrc); do
    cp bash_utility/configuration_files/$configuration_file ~/.$configuration_file
.bashrc: Change the path to this git AND the name of the specific bashrc of this environment
