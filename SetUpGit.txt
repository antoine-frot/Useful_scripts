# Set up the SSH key
ssh-keygen -t ed25519 -C "antoine.frot@orange.fr" (Use default)
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
cat ~/.ssh/id_ed25519.pub
git remote set-url origin git@github.com:antoine-frot/Useful_scripts

# Set up the .bashrc
cp ~/.bashrc bash_utility/configuration_files/bashrc_name_of_the_machine
cp bash_utility/configuration_files/bashrc to ~/.bashrc
Change the name of the bashrc specific of this environment in the new .bashrc
